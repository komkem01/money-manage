# 🎉 NotificationModal Component Guide

## 📋 ภาพรวม

NotificationModal เป็น Component สำหรับแสดงการแจ้งเตือนแบบ Modal ที่สวยงามและใช้งานง่าย รองรับหลายประเภทการแจ้งเตือน

## 🛠️ การติดตั้งและใช้งาน

### 1. Import Component และ Hook

```tsx
import NotificationModal from '@/components/NotificationModal';
import { useNotification } from '@/components/useNotification';
```

### 2. ใช้งาน Hook ใน Component

```tsx
const {
  notification,
  isOpen,
  hideNotification,
  showSuccess,
  showError,
  showWarning,
  showInfo,
  showConfirmation,
} = useNotification();
```

### 3. เพิ่ม Modal ใน JSX

```tsx
{notification && (
  <NotificationModal
    isOpen={isOpen}
    onClose={hideNotification}
    type={notification.type}
    title={notification.title}
    message={notification.message}
    autoClose={notification.autoClose}
    autoCloseDelay={notification.autoCloseDelay}
    showConfirmButton={notification.showConfirmButton}
    confirmButtonText={notification.confirmButtonText}
    showCancelButton={notification.showCancelButton}
    cancelButtonText={notification.cancelButtonText}
    onConfirm={notification.onConfirm}
    onCancel={notification.onCancel}
  />
)}
```

## 🎨 ประเภทการแจ้งเตือนที่รองรับ

### ✅ Success (สำเร็จ)
- สีเขียว
- ใช้สำหรับ: เข้าสู่ระบบสำเร็จ, บันทึกข้อมูลสำเร็จ, ลบข้อมูลสำเร็จ

```tsx
showSuccess(
  "บันทึกสำเร็จ!",
  "ข้อมูลของคุณถูกบันทึกเรียบร้อยแล้ว",
  {
    autoClose: true,
    autoCloseDelay: 3000
  }
);
```

### ❌ Error (ข้อผิดพลาด)
- สีแดง
- ใช้สำหรับ: ข้อผิดพลาดในการเข้าสู่ระบบ, ข้อมูลไม่ถูกต้อง, เซิร์ฟเวอร์ไม่ตอบสนอง

```tsx
showError(
  "เข้าสู่ระบบไม่สำเร็จ",
  "อีเมลหรือรหัสผ่านไม่ถูกต้อง กรุณาลองใหม่อีกครั้ง"
);
```

### ⚠️ Warning (คำเตือน)
- สีเหลือง
- ใช้สำหรับ: การยืนยันการดำเนินการ, คำเตือนก่อนลบข้อมูล

```tsx
showWarning(
  "แบตเตอรี่ใกล้หมด",
  "แบตเตอรี่เหลือ 15% กรุณาชาร์จโทรศัพท์ของคุณ"
);
```

### ℹ️ Info (ข้อมูล)
- สีน้ำเงิน
- ใช้สำหรับ: ข้อมูลทั่วไป, คำแนะนำ, อัปเดต

```tsx
showInfo(
  "คุณสมบัติใหม่!",
  "ตอนนี้คุณสามารถกรองข้อมูลตามเดือนได้แล้ว"
);
```

### 🔄 Confirmation (ยืนยัน)
- สีเหลือง + ปุ่ม 2 ปุ่ม
- ใช้สำหรับ: ยืนยันการออกจากระบบ, ยืนยันการลบข้อมูล

```tsx
showConfirmation(
  "ยืนยันการลบ",
  "คุณต้องการลบรายการนี้หรือไม่? การดำเนินการนี้ไม่สามารถย้อนกลับได้",
  () => {
    // ฟังก์ชันที่จะทำงานเมื่อกด "ยืนยัน"
    deleteItem();
  },
  {
    confirmButtonText: "ลบ",
    cancelButtonText: "ยกเลิก"
  }
);
```

## ⚙️ ตัวเลือกการปรับแต่ง

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `autoClose` | boolean | false | ปิดอัตโนมัติ |
| `autoCloseDelay` | number | 3000 | เวลาปิดอัตโนมัติ (มิลลิวินาที) |
| `showConfirmButton` | boolean | true | แสดงปุ่มยืนยัน |
| `confirmButtonText` | string | "ตกลง" | ข้อความปุ่มยืนยัน |
| `showCancelButton` | boolean | false | แสดงปุ่มยกเลิก |
| `cancelButtonText` | string | "ยกเลิก" | ข้อความปุ่มยกเลิก |
| `onConfirm` | function | - | Callback เมื่อกดยืนยัน |
| `onCancel` | function | - | Callback เมื่อกดยกเลิก |

## 🎯 ตัวอย่างการใช้งานในสถานการณ์จริง

### 1. การเข้าสู่ระบบ (Login)

```tsx
// สำเร็จ
showSuccess(
  "เข้าสู่ระบบสำเร็จ!",
  `ยินดีต้อนรับ ${userInfo.name}`,
  {
    autoClose: true,
    autoCloseDelay: 2000,
    onConfirm: () => router.push('/dashboard')
  }
);

// ล้มเหลว
showError(
  "เข้าสู่ระบบไม่สำเร็จ",
  "อีเมลหรือรหัสผ่านไม่ถูกต้อง กรุณาตรวจสอบและลองใหม่อีกครั้ง"
);
```

### 2. การลงทะเบียน (Register)

```tsx
// ข้อมูลไม่ถูกต้อง
showError("รหัสผ่านไม่ตรงกัน", "กรุณาตรวจสอบรหัสผ่านให้ตรงกัน");

// สำเร็จ
showSuccess(
  "ลงทะเบียนสำเร็จ!",
  `ยินดีต้อนรับ ${userData.firstname}! บัญชีของคุณถูกสร้างเรียบร้อยแล้ว`,
  {
    autoClose: true,
    autoCloseDelay: 2000,
    onConfirm: () => router.push('/dashboard')
  }
);
```

### 3. การออกจากระบบ (Logout)

```tsx
showConfirmation(
  "ยืนยันการออกจากระบบ",
  "คุณต้องการออกจากระบบหรือไม่? คุณจะต้องเข้าสู่ระบบใหม่อีกครั้ง",
  () => {
    // Logout logic
    removeAuthToken();
    removeUserData();
    showSuccess(
      "ออกจากระบบสำเร็จ",
      "ขอบคุณที่ใช้บริการ เราหวังว่าจะได้พบคุณอีกครั้ง",
      {
        autoClose: true,
        autoCloseDelay: 2000,
        onConfirm: () => router.push('/login')
      }
    );
  },
  {
    confirmButtonText: "ออกจากระบบ",
    cancelButtonText: "ยกเลิก"
  }
);
```

### 4. การลบข้อมูล (Delete)

```tsx
showConfirmation(
  "ยืนยันการลบ",
  "คุณต้องการลบรายการนี้หรือไม่? การดำเนินการนี้ไม่สามารถย้อนกลับได้",
  async () => {
    try {
      await deleteTransaction(id);
      showSuccess("ลบสำเร็จ", "รายการถูกลบเรียบร้อยแล้ว");
      // Refresh data
    } catch (error) {
      showError("ลบไม่สำเร็จ", "เกิดข้อผิดพลาดในการลบรายการ");
    }
  },
  {
    type: 'error',
    confirmButtonText: "ลบ",
    cancelButtonText: "ยกเลิก"
  }
);
```

## 🎨 คุณสมบัติพิเศษ

- **🎭 Animation เสน้าไฟ**: Modal มี animation สวยงามเมื่อเปิด-ปิด
- **⌨️ Keyboard Support**: กด Escape เพื่อปิด Modal
- **📱 Responsive**: ตอบสนองบนทุกขนาดหน้าจอ
- **🔒 Body Scroll Lock**: ป้องกันการเลื่อนหน้าเว็บเมื่อ Modal เปิด
- **⏱️ Auto Close Progress Bar**: แสดง Progress Bar เมื่อปิดอัตโนมัติ
- **🎨 Theme Colors**: สีที่สอดคล้องกับประเภทการแจ้งเตือน

## 🚀 การใช้งานขั้นสูง

### Custom Callback Functions

```tsx
showSuccess(
  "อัปโหลดสำเร็จ",
  "ไฟล์ของคุณถูกอัปโหลดเรียบร้อยแล้ว",
  {
    confirmButtonText: "ดูไฟล์",
    onConfirm: () => {
      // เปิดไฟล์ที่อัปโหลด
      window.open('/uploads/myfile.pdf', '_blank');
    }
  }
);
```

### Chaining Notifications

```tsx
const handleComplexAction = async () => {
  try {
    showInfo("กำลังประมวลผล", "กรุณารอสักครู่...");
    
    await processData();
    
    showSuccess(
      "ประมวลผลสำเร็จ",
      "ข้อมูลของคุณถูกประมวลผลเรียบร้อยแล้ว",
      {
        onConfirm: () => {
          showInfo(
            "ขั้นตอนถัดไป",
            "คุณต้องการดำเนินการขั้นตอนถัดไปหรือไม่?"
          );
        }
      }
    );
  } catch (error) {
    showError("เกิดข้อผิดพลาด", error.message);
  }
};
```

## 📝 หมายเหตุการใช้งาน

1. ใช้ `showSuccess` สำหรับการดำเนินการที่สำเร็จ
2. ใช้ `showError` สำหรับข้อผิดพลาดที่ผู้ใช้ต้องรับทราب
3. ใช้ `showConfirmation` สำหรับการดำเนินการที่ไม่สามารถย้อนกลับได้
4. ใช้ `showWarning` สำหรับคำเตือนที่ผู้ใช้ควรระวัง
5. ใช้ `showInfo` สำหรับข้อมูลทั่วไปหรือคำแนะนำ

Component นี้จะช่วยให้ UX ของแอปพลิเคชันดูเป็นมืออาชีพและใช้งานง่ายมากขึ้น! 🎉